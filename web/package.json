{
  "name": "@whitehawkcec/whitehawk-proto",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsdown src-gen/**/*.ts --format cjs --format esm --minify --clean --sourcemap --dts"
  },
  "dependencies": {
    "@protobuf-ts/runtime": "^2.11.1",
    "@protobuf-ts/runtime-rpc": "^2.11.1",
    "google-protobuf": "^3.21.4"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "tsdown": "^0.14.1",
    "typescript": "^5.9.2"
  }
}
