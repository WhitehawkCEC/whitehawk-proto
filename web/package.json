{
  "name": "@whitehawkcec/whitehawk-proto",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "cross-env NODE_OPTIONS=--max-old-space-size=8192 tsup src-gen/**/*.ts --format cjs,esm --dts --minify --clean && tsc --project tsconfig.esm.json"
  },
  "dependencies": {
    "@protobuf-ts/runtime": "^2.8.1",
    "@protobuf-ts/runtime-rpc": "^2.8.1",
    "google-protobuf": "^3.21.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "tsup": "^6.6.3",
    "typescript": "^4.8.4"
  }
}
