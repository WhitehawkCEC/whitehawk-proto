syntax = "proto3";

package whitehawk.proto.integration.cyber_one.domain.v1;

message ApiKey {
  // 40 characters (lowercase letters or numbers)
  // Pattern: [a-z0-9]{40}
  string value = 1;
}

message CyberOneAsset {
  int32 id = 1;
  string reference_id = 2;
  string name = 3;
  optional string domain = 4;
}
